openapi: "3.0.0"

info:
  title: Memstack API
  version: 1.0.0

servers:
  - description: Local
    url: http://localhost:3000

  - description: Development
    url: https://dev.memstack.io/api

paths:
  /cards:
    post:
      summary: Add a card
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Card"

      responses:
        200:
          description: Created Card
  /stacks:
    post:
      summary: Add a stacks
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Stack"

      responses:
        200:
          description: Created Stack

    get:
      summary: Gets all stacks
      responses:
        200:
          description: List of Cards
  /stacks/{stackid}/cards/{cardid}:
    post:
      summary: Adds a card to a stack
      parameters:
        - in: path
          name: stackid
          description: "The stack to add the card too"
          schema:
            type: string
          required: true
        - in: path
          name: cardid
          description: "The card to be added"
          schema:
            type: string
          required: true
          

components:
  schemas:
    Card:
      type: object
      properties:
        front:
          type: string
        back:
          type: string
      required:
        - front
        - back

    Stack:
      type: object
      properties:
        title:
          type: string
      required:
        - title

    StackWithId:
      allOf: # Combines the Stack and the inline model
        - $ref: "#/components/schemas/Stack"
        - type: object
          required:
            - id
          properties:
            id:
              type: string
